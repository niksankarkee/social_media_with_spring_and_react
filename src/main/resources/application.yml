server:
  port: 8080
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/niksan_social_media
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
